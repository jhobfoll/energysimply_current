<%= form_for(@click_tracking) do |f| %>
  <% if @click_tracking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@click_tracking.errors.count, "error") %> prohibited this click_tracking from being saved:</h2>

      <ul>
      <% @click_tracking.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :zip %><br>
    <%= f.text_field :zip %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
